{
  "name": "my-github-pages-site",
  "version": "1.0.0",
  "description": "Static site built from MD files",
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && npx serve docs"
  },
  "dependencies": {
    "marked": "^9.0.0"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
}